import datetime
import re
#open https log file
f = open('file.log')

dates = 

lines = f.readlines()
count=0 #set count to 0
for line in lines:
    count=count+1
print(count)

matches = re.findall(r'(\d\d+/\d\d\d+/\d\d\d\d+)',lines)
print(matches)
